//import moduleName from 'module'

const initialState = {
    test: 'vini'
}

export default function (state = initialState, action) {
    switch(action.type){
        default:
        return state

    }

}